<Window x:Class="RtlTerminal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="RtlTerminal"
        Icon="RtlTerminal.ico"
        UseLayoutRounding="True"
        Width="980"
        Height="620"
        Background="#0c0c0c"
        WindowStyle="None"
        ResizeMode="CanResize"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        SizeChanged="Window_SizeChanged"
        PreviewKeyDown="Window_PreviewKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            ResizeBorderThickness="6"
                            CornerRadius="8"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="DarkSubmenuItemStyle"
               TargetType="MenuItem">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Foreground"
                    Value="#f0f0f0" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Setter Property="Padding"
                    Value="8,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid Background="Transparent">
                            <Border x:Name="ItemBackground"
                                    Margin="3,1"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="22" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="18" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock x:Name="CheckMark"
                                               Text="✓"
                                               Foreground="{TemplateBinding Foreground}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Visibility="Collapsed" />
                                    <ContentPresenter Grid.Column="1"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      VerticalAlignment="Center" />
                                    <TextBlock x:Name="SubmenuArrow"
                                               Grid.Column="2"
                                               Text="›"
                                               FontSize="16"
                                               Foreground="{TemplateBinding Foreground}"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               Visibility="Collapsed" />
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup"
                                   Placement="Right"
                                   HorizontalOffset="-3"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border MinWidth="180"
                                        Padding="3"
                                        Background="#252526"
                                        BorderBrush="#3f3f46"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                    <ScrollViewer CanContentScroll="True"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Disabled">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Role"
                                     Value="SubmenuHeader">
                                <Setter TargetName="SubmenuArrow"
                                        Property="Visibility"
                                        Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsChecked"
                                     Value="True">
                                <Setter TargetName="CheckMark"
                                        Property="Visibility"
                                        Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsHighlighted"
                                     Value="True">
                                <Setter TargetName="ItemBackground"
                                        Property="Background"
                                        Value="#3a3a3a" />
                                <Setter Property="Foreground"
                                        Value="#ffffff" />
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter Property="Opacity"
                                        Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkTopLevelMenuItemStyle"
               TargetType="MenuItem">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Foreground"
                    Value="#f0f0f0" />
            <Setter Property="ItemContainerStyle"
                    Value="{StaticResource DarkSubmenuItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid>
                            <Border x:Name="HeaderBackground"
                                    Margin="2,2"
                                    Padding="9,3"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center" />
                            </Border>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   VerticalOffset="-2"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border MinWidth="190"
                                        Padding="3"
                                        Background="#252526"
                                        BorderBrush="#3f3f46"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                    <ScrollViewer CanContentScroll="True"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Disabled">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted"
                                     Value="True">
                                <Setter TargetName="HeaderBackground"
                                        Property="Background"
                                        Value="#3a3a3a" />
                                <Setter Property="Foreground"
                                        Value="#ffffff" />
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen"
                                     Value="True">
                                <Setter TargetName="HeaderBackground"
                                        Property="Background"
                                        Value="#414141" />
                                <Setter Property="Foreground"
                                        Value="#ffffff" />
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter Property="Opacity"
                                        Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkContextMenuStyle"
               TargetType="ContextMenu">
            <Setter Property="Background"
                    Value="#252526" />
            <Setter Property="Foreground"
                    Value="#f0f0f0" />
            <Setter Property="BorderBrush"
                    Value="#3f3f46" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Padding"
                    Value="3" />
            <Setter Property="ItemContainerStyle"
                    Value="{StaticResource DarkSubmenuItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border MinWidth="180"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer CanContentScroll="True"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Disabled">
                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCaptionButtonStyle"
               TargetType="Button">
            <Setter Property="Foreground"
                    Value="#f0f0f0" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Center" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CaptionBackground"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="CaptionBackground"
                                        Property="Background"
                                        Value="#404040" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="CaptionBackground"
                                        Property="Background"
                                        Value="#505050" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver"
                                               Value="True" />
                                    <Condition Property="Tag"
                                               Value="Close" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="CaptionBackground"
                                        Property="Background"
                                        Value="#c42b1c" />
                                <Setter Property="Foreground"
                                        Value="#ffffff" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsPressed"
                                               Value="True" />
                                    <Condition Property="Tag"
                                               Value="Close" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="CaptionBackground"
                                        Property="Background"
                                        Value="#a92316" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkTitleButtonStyle"
               TargetType="Button">
            <Setter Property="Foreground"
                    Value="#f0f0f0" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#3a3a3a" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#464646" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel Background="#0c0c0c">
        <Grid DockPanel.Dock="Top"
              Height="40"
              Background="#2b2b2b">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="138" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <ScrollViewer HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="TabStrip"
                                Orientation="Horizontal"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True" />
                </ScrollViewer>
            </DockPanel>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Width="46"
                        Style="{StaticResource WindowCaptionButtonStyle}"
                        FontSize="15"
                        Content="—"
                        Click="MinimizeButton_Click" />
                <Button x:Name="MaximizeButton"
                        Width="46"
                        Style="{StaticResource WindowCaptionButtonStyle}"
                        FontSize="14"
                        Content="□"
                        Click="MaximizeButton_Click" />
                <Button Width="46"
                        Style="{StaticResource WindowCaptionButtonStyle}"
                        Tag="Close"
                        FontSize="13"
                        Content="✕"
                        Click="CloseWindowButton_Click" />
            </StackPanel>
        </Grid>

        <Menu DockPanel.Dock="Top"
              Height="26"
              Background="#202020"
              Foreground="#f0f0f0"
              ItemContainerStyle="{StaticResource DarkTopLevelMenuItemStyle}">
            <MenuItem Header="_File">
                <MenuItem x:Name="LastDirectoriesMenuItem"
                          Header="_Last directories"
                          SubmenuOpened="LastDirectoriesMenuItem_SubmenuOpened">
                    <MenuItem Header="No recent directories"
                              IsEnabled="False" />
                </MenuItem>
                <Separator />
                <MenuItem Header="_Export session..."
                          Click="ExportSessionMenuItem_Click" />
                <Separator />
                <MenuItem Header="E_xit"
                          Click="ExitMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Font settings..."
                          Click="FontSettingsMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem x:Name="SmartRtlMenuItem"
                          Header="_Smart RTL"
                          IsCheckable="True"
                          ToolTip="Use right-to-left layout only for lines containing RTL letters"
                          Checked="SmartRtlMenuItem_Changed"
                          Unchecked="SmartRtlMenuItem_Changed" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem x:Name="ContextMenuIntegrationMenuItem"
                          Header="Add _Open in RtlTerminal"
                          IsCheckable="True"
                          Click="ContextMenuIntegrationMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_Guide"
                          Click="GuideMenuItem_Click" />
                <MenuItem x:Name="CheckForUpdatesMenuItem"
                          Header="Check for _updates..."
                          Click="CheckForUpdatesMenuItem_Click" />
                <Separator />
                <MenuItem Header="_About Rtl Terminal"
                          Click="AboutMenuItem_Click" />
            </MenuItem>
        </Menu>

        <RichTextBox x:Name="TerminalTextBox"
                     Background="#0c0c0c"
                     Foreground="#e6e6e6"
                     BorderThickness="0"
                     Padding="10"
                     FontFamily="Cascadia Mono, Consolas, Segoe UI Emoji, Segoe UI Symbol, Segoe UI Historic"
                     FontSize="15"
                     HorizontalScrollBarVisibility="Disabled"
                     VerticalScrollBarVisibility="Visible"
                     ScrollViewer.IsDeferredScrollingEnabled="False"
                     IsReadOnly="True"
                     IsDocumentEnabled="True"
                     IsReadOnlyCaretVisible="False"
                     Cursor="IBeam"
                     IsUndoEnabled="False"
                     SpellCheck.IsEnabled="False"
                     FlowDirection="LeftToRight"
                     CaretBrush="#e6e6e6"
                     TextOptions.TextFormattingMode="Ideal"
                     TextOptions.TextRenderingMode="ClearType"
                     Typography.StandardLigatures="False"
                     Typography.ContextualLigatures="False"
                     Typography.DiscretionaryLigatures="False"
                     Typography.Kerning="False"
                     Typography.NumeralAlignment="Tabular"
                     ScrollViewer.ScrollChanged="TerminalTextBox_ScrollChanged"
                     PreviewKeyDown="TerminalTextBox_PreviewKeyDown"
                     PreviewMouseDown="TerminalTextBox_PreviewMouseDown"
                     PreviewMouseUp="TerminalTextBox_PreviewMouseUp"
                     PreviewMouseMove="TerminalTextBox_PreviewMouseMove"
                     PreviewMouseWheel="TerminalTextBox_PreviewMouseWheel"
                     PreviewMouseRightButtonDown="TerminalTextBox_PreviewMouseRightButtonDown"
                     GotKeyboardFocus="TerminalTextBox_GotKeyboardFocus"
                     LostKeyboardFocus="TerminalTextBox_LostKeyboardFocus"
                     PreviewTextInput="TerminalTextBox_PreviewTextInput">
            <FlowDocument PagePadding="0"
                          ColumnWidth="Infinity"
                          LineHeight="18"
                          LineStackingStrategy="BlockLineHeight"
                          TextAlignment="Left" />
        </RichTextBox>
    </DockPanel>
</Window>
